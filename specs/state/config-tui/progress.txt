GBM Config TUI - Task Progress
==============================
Created: 2026-01-27
PRD: prd-config-tui.md

Task Completion Status:
- [x] Sidebar navigation component renders correctly (collapsed, expanded, badges)
- [x] RootModel (ConfigModel) delegates to Navigator correctly
- [x] gbm config command registered and launches TUI without errors
- [x] Basics section renders with default_branch and worktrees_dir fields
- [x] TextInput validators prevent invalid branch names and directory paths
- [x] Save/discard flows work correctly from Basics section
- [x] JIRA enable/disable toggle works and shows/hides subsections
- [x] JIRA subsections (Server, Filters, Attachments, Markdown) render with all fields editable
- [x] FileCopy Rules table renders with add/edit/delete modals
- [x] Filepicker navigation works (↑↓, Enter to select, Escape to cancel)
- [x] Worktrees table with add/edit/delete modals
- [x] Validation error overlays appear on invalid save attempts
- [x] Dark and light themes both render correctly
- [x] Help screen shows keyboard shortcuts via ? key
- [x] All sections integrated in one ConfigModel with persistent state
- [x] Storybook covers >80% of components/sections in multiple states
- [x] Unit tests >80% coverage for core ConfigModel and field validation
- [x] E2E happy path test: load config → edit → save → verify file
- [x] No breaking changes to existing commands
- [ ] CLI help and in-app documentation complete

Completed Items:
1. Sidebar navigation component (pkg/tui/sidebar.go, pkg/tui/sidebar_test.go)
2. ConfigModel root component (pkg/tui/config_model.go, pkg/tui/config_model_test.go)
3. Config CLI command (cmd/service/config_cmd.go, registered in root.go)
4. BasicsForm component (pkg/tui/config/basics_form.go, pkg/tui/config/basics_form_test.go)
5. Save/Discard flows:
    - Implemented 's' key handler to save form data via OnSave callback
    - Implemented 'q' key handler to show discard confirmation modal
    - Created Confirm field for "Discard unsaved changes?" modal
    - Added key handlers for y/n responses (yes/no to discard)
    - Tests: SaveFlow, DiscardFlow, KeepEditingFlow with teatest
    - All 12 tests passing
6. JIRA Form with Enable/Disable Toggle:
     - Created JiraForm component (pkg/tui/config/jira_form.go)
     - Implements enable/disable toggle using Confirm field
     - Shows/hides server config fields (host, username, api_token) based on enabled state
     - Added SetMasked() to TextInput for masking sensitive API tokens
     - Full teatest coverage: 7 tests passing
     - Test cases: disabled/enabled renders, save flow, discard/keep editing, navigation, escape key

7. JIRA Subsections (Server, Filters, Attachments, Markdown):
     - Enhanced JiraForm to include all 4 subsections with full field coverage
     - Server: host, username, api_token (3 fields)
     - Filters: status, priority, type (3 fields)  
     - Attachments: enabled toggle, max_size, directory (3 fields)
     - Markdown: include_comments, include_attachments, use_relative_links, filename_pattern (4 fields)
     - Total 14 fields (1 enable toggle + 13 subsection fields)
     - Tab/Shift+Tab navigation cycles through all 14 fields
     - GetValue() returns all subsection field values as map
     - Tests verify all subsections render and return values
     - Field improvements: Added SetValue() to Confirm, fixed TextInput.GetValue() to return pre-populated defaults
     - All 17 JiraForm tests passing

     8. FileCopy Rules Table with Add/Edit/Delete Modals:
     - Created FileCopyForm component (pkg/tui/config/filecopy_form.go)
     - Table displays source_worktree and files columns using existing Table component
     - ModalState enum tracks current modal (None, Add, Edit, Delete, Discard)
     - 'a' key opens Add modal with source_worktree and files fields
     - 'e' key opens Edit modal with pre-populated values from selected rule
     - 'd' key opens Delete confirmation modal
     - Escape in modal closes modal without changes
     - Save/discard flows match existing form patterns
     - Helper functions: formatFilesPreview(), parseFilesList()
     - Tests: 15 tests covering render, modals, save/delete/discard flows
     - All tests passing

9. Filepicker Navigation:
     - Created FilePicker field component (pkg/tui/fields/filepicker.go)
     - Wraps charmbracelet/bubbles/filepicker for consistent Field interface
     - Added ModalFilePicker state to FileCopyForm
     - Press 'b' in Files field to open filepicker modal
     - ↑↓ navigates file tree, → opens dirs, ← goes to parent
     - Enter selects file(s) and adds to files list
     - Escape cancels filepicker and returns to rule form
     - Multi-select mode with Space to add files, Enter to confirm
     - Selected files display in filepicker view and populate files field
     - Tests: 24 FilePicker unit tests, 2 integration tests for FileCopyForm
     - All tests passing

10. Worktrees Table with Add/Edit/Delete Modals:
     - Created WorktreesForm component (pkg/tui/config/worktrees_form.go)
     - Table displays name, branch, merge_into columns using existing Table component
     - WorktreeModalState enum tracks modal state (None, Add, Edit, Delete, Discard)
     - 'a' key opens Add modal with name, branch, merge_into, description fields
     - 'e' key opens Edit modal with pre-populated values from selected worktree
     - 'd' key opens Delete confirmation modal
     - Escape in modal closes without changes
     - Validation: name required (alphanumeric, -, _), branch required, duplicate name check
     - Validation errors displayed inline in modal
     - Tab/Shift+Tab cycles through 4 modal fields
     - Save/discard flows match existing form patterns
     - Tests: 25 tests covering render, modals, validation, save/delete/discard flows
     - All tests passing

11. All Sections Integrated in One ConfigModel with Persistent State:
     - Added ConfigState struct to hold all editable config values in memory
     - ConfigState includes: Basics (DefaultBranch, WorktreesDir), JIRA settings (14 fields), FileCopyRules, Worktrees
     - Added FormFactory callback pattern to avoid import cycles between pkg/tui and pkg/tui/config
     - ConfigModel options: WithInitialState, WithOnSave, WithOnReset, WithFormFactory
     - ConfigModel handles SidebarSelectionMsg to create and push section forms
     - ConfigModel handles BackBoundaryMsg to pop forms and return to sidebar
     - Forms update shared state via OnSave callbacks - edits persist across navigation
     - Global 's' key at sidebar saves all changes to .gbm/config.yaml
     - Global 'r' key at sidebar resets in-memory state from file
     - config_cmd.go: configToState() and stateToConfig() convert between Config and ConfigState
     - View shows help footer at sidebar level: "↑↓=navigate  Enter=select  s=save  r=reset  q=quit [modified]"
     - Dirty state tracking shows [modified] indicator when changes are pending
     - Tests: 12 new tests covering state, form factory, navigation, reset, save, dirty tracking
     - All tests passing
     **Learnings:**
     - Used FormFactory callback pattern to avoid import cycles (pkg/tui can't import pkg/tui/config)
     - JiraFilters struct uses `Type` field, not `IssueType`
     - State conversions between service.Config and tui.ConfigState happen in cmd/service layer

12. Validation Error Overlays:
     - Created ValidationOverlay component (pkg/tui/fields/validation_overlay.go)
     - Displays validation errors in a bordered box with red styling
     - Shows title "Validation Errors", bullet-pointed error list, dismissal instructions
     - Handles Escape, 'b', and Enter keys to dismiss overlay
     - Added RunValidator() method to TextInput to validate without requiring Enter key
     - Added SetError() method to TextInput for setting error state programmatically
     - Updated BasicsForm with Validate() method that runs all field validators
     - Updated JiraForm with Validate() method for server fields when JIRA is enabled
     - Save ('s' key) now validates before saving - shows overlay if errors exist
     - After dismissing overlay, errors remain highlighted on fields
     - Save errors (from onSave callback) also shown in overlay with "Save Error" title
     - Tests: 6 new tests covering Validate, ValidationOverlay, ValidationOverlayDismiss, SaveWithValidData
     - All tests passing
     **Learnings:**
     - Type assertion pattern: `textField, ok := f.field.(*fields.TextInput)` to access concrete methods
     - Overlay rendering priority: validation errors > discard confirmation > normal form view

13. Help Screen with Keyboard Shortcuts:
     - Created HelpOverlay component (pkg/tui/help_overlay.go) in tui package (not fields) to avoid import cycles
     - Displays organized keyboard shortcuts in a bordered box with rounded corners
     - Groups shortcuts by context: Navigation, Sidebar, Table Forms, Modals, FilePicker
     - Each shortcut shows key and description with aligned formatting
     - Dismisses on Escape, '?', or Enter keys
     - Implemented builder pattern: NewHelpOverlay().WithTheme().WithWidth().WithHeight()
     - Integrated into ConfigModel for sidebar-level help (depth == 1)
     - Updated all 4 forms (BasicsForm, JiraForm, FileCopyForm, WorktreesForm) with '?' key handler
     - Added ModalHelp state to FileCopyForm and WorktreeModalHelp to WorktreesForm
     - Forms show "?=help" in footer text
     - ConfigModel footer updated: "↑↓=navigate  Enter=select  s=save  r=reset  ?=help  q=quit"
     - Tests: 24 HelpOverlay tests + 6 ConfigModel help tests (all passing)
     - Files changed: pkg/tui/help_overlay.go, pkg/tui/help_overlay_test.go, pkg/tui/config_model.go,
       pkg/tui/config_model_test.go, pkg/tui/config/basics_form.go, pkg/tui/config/jira_form.go,
       pkg/tui/config/filecopy_form.go, pkg/tui/config/worktrees_form.go
     **Learnings:**
     - HelpOverlay must be in pkg/tui (not pkg/tui/fields) because ConfigModel can't import pkg/tui/fields (import cycle)
     - Forms with modal state enums need a new Help modal state (ModalHelp, WorktreeModalHelp)
     - Reused overlay pattern from ValidationOverlay but with gray border instead of red

14. No Breaking Changes to Existing Commands:
     - Verified all existing worktree commands still work correctly
     - `gbm wt list --json` outputs worktree data correctly
     - `gbm wt add` with --dry-run shows expected git commands
     - `gbm wt remove` error handling unchanged for nonexistent worktrees
     - `gbm wt switch` works correctly with existing worktrees
     - `gbm sync --dry-run` shows expected sync behavior
     - `gbm --help` lists config command alongside existing commands
     - `gbm config --help` shows proper command description
     - All tests pass (`just check` - tests PASS)
     - Pre-existing lint issues (cyclomatic complexity) unchanged
     **Learnings:**
     - Verification task - no code changes required
     - Config TUI is additive and doesn't touch existing command paths

15. E2E Happy Path Test: Load Config → Edit → Save → Verify File:
     - Created comprehensive E2E tests in e2e_test.go (7 tests)
     - Tests cover: happy path, edit basics, edit JIRA, preserve fields, valid YAML, worktrees section, file copy rules
     - Created integration E2E tests in cmd/service/config_e2e_test.go (6 tests)
     - Tests use real ConfigModel with service layer callbacks
     - Test flow: load config → navigate sections → edit state → save → verify file
     - Added MarkDirty(), ClearDirty(), IsDirty() methods to ConfigState for test access
     - Tests verify:
       * Config file loads correctly
       * Navigation through all sections works
       * Edits persist after save
       * Original fields are preserved when editing others
       * JIRA, FileCopy, and Worktrees sections save correctly
       * Reset functionality restores original state
     - All 13 E2E tests passing
     - Files changed:
       * e2e_test.go (added 7 tests)
       * cmd/service/config_e2e_test.go (new file, 6 tests)
       * pkg/tui/config_model.go (added MarkDirty/ClearDirty/IsDirty methods)
     **Learnings:**
     - Interactive TUI cannot be tested directly in automated tests (requires /dev/tty)
     - Use ConfigModel directly with Update() calls to simulate user interactions
     - Service layer callbacks (onSave, onReset) enable testing the full save/load flow
     - FormFactory pattern allows section forms to be created without import cycles

16. Unit Tests >80% Coverage for ConfigModel and Field Validation:
     - Created validation_overlay_test.go for fields package (18 tests)
     - Tests cover: NewValidationOverlay, WithTheme, WithTitle, WithWidth, WithHeight,
       Init, Update dismissal (Escape/b/Enter), WindowSize, View rendering, Errors()
     - Created builder_methods_test.go for fields package (30+ tests)
     - Tests cover: TextInput (WithPlaceholder, SetMasked, Error, Skip, WithTheme,
       WithWidth, WithHeight, GetKey, RunValidator, SetError)
     - Tests cover: Confirm (SetValue, Error, Skip, WithTheme, WithWidth, WithHeight, GetKey)
     - Tests cover: Selector (Error, Skip, WithTheme, WithWidth, WithHeight, GetKey)
     - Tests cover: Filterable (Error, Skip, WithTheme, WithWidth, GetKey)
     - Tests cover: FilePicker (Init, Update)
     - Created help_overlay_test.go for config package (25+ tests)
     - Tests cover: BasicsForm help overlay (show, dismiss Escape/?/Enter, non-key messages)
     - Tests cover: JiraForm help overlay, validation overlay, Enabled() accessor
     - Tests cover: WorktreesForm help overlay, Init()
     - Tests cover: FileCopyForm help overlay
     - Coverage improvements:
       * pkg/tui/fields: 73.5% → 91.8%
       * pkg/tui/config: 73.0% → 80.0%
     - All tests passing
     - Files changed:
       * pkg/tui/fields/validation_overlay_test.go (new file)
       * pkg/tui/fields/builder_methods_test.go (new file)
       * pkg/tui/config/help_overlay_test.go (new file)
     **Learnings:**
     - Use require.EqualError/require.NoError for error assertions (testifylint rule)
     - Filterable uses Option type (same as Selector) with 4 args: key, title, description, options
     - Help overlay handlers follow same pattern across all forms: dismiss on esc/?/enter

17. Dark and Light Themes Both Render Correctly:
     - Updated theme.go to use lipgloss.AdaptiveColor for all theme colors
     - AdaptiveColor automatically detects terminal background (light/dark) and uses appropriate color
     - Added new adaptive colors to Theme struct: Accent, Muted, Highlight, Border, ErrorAccent,
       SuccessAccent, SelectedFg, SelectedBg, InputBg, InputFg, BlurredMuted, Cursor
     - Light theme uses: blue accent, darker grays, amber highlights
     - Dark theme uses: teal accent, lighter grays, gold highlights
     - Updated help_overlay.go to use theme.Accent for group headers, theme.Highlight for keys,
       theme.Border for box borders
     - Updated validation_overlay.go to use theme.ErrorAccent for border
     - Updated table.go to use theme.Cursor for filter input, theme.BlurredMuted for filter indicator
     - Updated confirm.go to use theme.SuccessAccent for "Yes" button, theme.ErrorAccent for "No",
       theme.InputFg for button text, theme.BlurredMuted for unselected buttons
     - Updated selector.go to use theme.Cursor for selector indicator, theme.Muted for dimmed style
     - Updated filterable.go to use theme.Cursor for cursor, theme.BlurredMuted for no-match,
       theme.Muted for dimmed selected items
     - All hardcoded ANSI color numbers (e.g., lipgloss.Color("212")) replaced with adaptive colors
     - All tests passing
     - Files changed:
       * pkg/tui/theme.go (major refactor)
       * pkg/tui/help_overlay.go
       * pkg/tui/fields/validation_overlay.go
       * pkg/tui/table.go
       * pkg/tui/fields/confirm.go
       * pkg/tui/fields/selector.go
       * pkg/tui/fields/filterable.go
     **Learnings:**
     - lipgloss.AdaptiveColor{Light: "#...", Dark: "#..."} automatically detects terminal background
     - Use hex colors (e.g., "#0077b6") for consistency and readability
     - Theme struct should expose adaptive colors for use by components that build custom styles
     - Keep color definitions centralized in DefaultTheme() for easy adjustment

18. Storybook Covers >80% of Components/Sections in Multiple States:
     - Split storybook into two files to stay under 800-line limit
     - cmd/storybook/main.go: core stories (Table, TextInput, Selector, Confirm, Filterable, Wizard)
     - cmd/storybook/config_stories.go: config TUI stories (7 components, 28 variants)
     - Sidebar stories: AllCollapsed, AllExpanded, WithFocus, WithValidationErrors
     - BasicsForm stories: Empty, Populated, WithValidationErrors, DiscardConfirmation
     - JiraForm stories: Disabled, EnabledWithSubsections, ServerSection, WithValidationErrors
     - FileCopyForm stories: EmptyRules, WithRules, AddModal, EditModal, DeleteConfirmation
     - WorktreesForm stories: EmptyTable, PopulatedTable, AddModal, EditModal, DeleteConfirmation, WithValidationError
     - HelpOverlay stories: Default, CustomGroups
     - ValidationOverlay stories: SingleError, MultipleErrors, SaveError
     - Total: 13 components, 44 variants covering >80% of component states
     - All stories use tui.DefaultTheme() which provides adaptive light/dark theming
     - All tests passing
     - Files changed:
       * cmd/storybook/main.go (removed unused config import)
       * cmd/storybook/config_stories.go (new file, 538 lines)
     **Learnings:**
     - Storybook stories can have Play functions for automated interactions (pc.Key, pc.Wait, pc.AssertContains)
     - Split files by concern to avoid 800-line limit while keeping related stories together
     - Model wrappers needed for components that return interfaces (sidebarModel, helpOverlayModel, etc.)
