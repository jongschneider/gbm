1. update gbm wt remove to move wt dir to Trash (with timestamp?) before runnning `git worktree remove` to be able to get it back.
2. add pull
3. add push
4. make (pull, push, remove) available to `gbm wt list`
5. add `*` to `gbm wt list` and filter to top for current wt